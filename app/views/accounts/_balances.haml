%p
  Balance:
  = number_to_currency @month.transactions.sum(:amount) + @month.start_amount
  Cleared balance:
  = number_to_currency @month.transactions.cleared.sum(:amount) + @month.start_amount
  Beginning balance:
  = @month.start_amount
